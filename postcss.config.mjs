 HEAD
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },

const config = {
  plugins: ["@tailwindcss/postcss"],
 origin/main
};

export default config;
